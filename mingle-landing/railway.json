{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile --ignore-scripts"
  },
  "deploy": {
    "startCommand": "npx ts-node stt-server.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
