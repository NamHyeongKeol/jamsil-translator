{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then exit 0; fi; git show --name-only --pretty=\"\" HEAD | grep -Eq '^(mingle-app/)' && exit 1 || exit 0"
}
